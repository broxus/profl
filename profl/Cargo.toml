[package]
name = "profl"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = "1.3"
crossbeam-channel = "0.5"
log = "0.4"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
env_logger = "0.9.0"

[features]
active = []
